{
  "name": "treeify-paths",
  "version": "1.0.2",
  "description": "list of file names becomes a tree of file descriptions",
  "main": "dist/treeify-paths.js",
  "scripts": {
    "build": "tsc --outDir dist treeify-paths.ts",
    "test": "mocha tests.js",
    "clean": "rimraf dist/*",
    "dev": "concurrently 'yarn run build -w' 'yarn run test -w'"
  },
  "repository": "github:khtdr/treeify-paths",
  "author": "oh kay",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.0.0",
    "mocha": "^9.2.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  }
}
